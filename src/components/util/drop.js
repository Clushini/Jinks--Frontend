import React from 'react';

const Drop = (props) => {
    return(
        <div className="dropmodal" style={{marginTop: props.margintop}}>
            test
        </div>
    )
}

export default Drop;